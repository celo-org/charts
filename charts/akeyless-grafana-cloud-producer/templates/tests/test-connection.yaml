apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "akeyless-grafana-cloud-producer.fullname" . }}-test-connection"
  labels:
    {{- include "akeyless-grafana-cloud-producer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "akeyless-grafana-cloud-producer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
