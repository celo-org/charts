{{- $replicas := .Values.replicaCount }}
{{ range $index, $e := until (int $replicas) }}
{{- with $.Values.services.pprof }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "op-reth.fullname" $ }}-pprof-{{ $index }}
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    service-type: pprof
    {{- include "op-reth.labels" $ | nindent 4 }}
spec:
  type: {{ .type }}
  publishNotReadyAddresses: {{ .publishNotReadyAddresses }}
  ports:
    - name: pprof
      port: {{ .port }}
      targetPort: pprof
  selector:
    {{- include "op-reth.selectorLabels" $ | nindent 4 }}
    statefulset.kubernetes.io/pod-name: {{ template "op-reth.fullname" $ }}-{{ $index }}
{{- end }}
{{- end }}