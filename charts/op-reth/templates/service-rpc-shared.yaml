{{- with .Values.services.rpc }}
{{- if .sharedServiceEnabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "op-reth.fullname" $ }}-shared-rpc
  {{- with .annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    service-type: rpc
    {{- include "op-reth.labels" $ | nindent 4 }}
spec:
  type: {{ .type }}
  {{- with .loadBalancerIP }}
  loadBalancerIP: {{ . }}
  {{- end }}
  {{- with .clusterIP }}
  clusterIP: {{ . }}
  {{- end }}
  {{- with .externalTrafficPolicy }}
  externalTrafficPolicy: {{ . }}
  {{- end }}
  {{- with .internalTrafficPolicy }}
  internalTrafficPolicy: {{ . }}
  {{- end }}
  publishNotReadyAddresses: false
  ports:
    - name: http
      port: {{ .httpPort }}
      targetPort: http
    {{- if and $.Values.config.ws.enabled (and (ne .httpPort .wsPort) (ne .Values.config.http.port .Values.config.ws.port)) }}
    - name: ws
      port: {{ .wsPort }}
      targetPort: ws
    {{- end }}
  selector:
    {{- include "op-reth.selectorLabels" $ | nindent 4 }}
{{- end }}
{{- end }}
