---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ultragreen-dashboard.fullname" . }}-footprint
  labels:
    {{- include "ultragreen-dashboard.labels" . | nindent 4 }}
    component: footprint
spec:
  replicas: {{ .Values.backend.footPrint.replicaCount }}
  selector:
    matchLabels:
      {{- include "ultragreen-dashboard.selectorLabels" . | nindent 6 }}
      component: footprint
  template:
    metadata:
      labels:
        {{- include "ultragreen-dashboard.selectorLabels" . | nindent 8 }}
        component: footprint
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "ultragreen-dashboard.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.backend.footPrint.image.repository }}:{{ .Values.backend.footPrint.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.backend.footPrint.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.backend.footPrint.service.port }}
              protocol: TCP
          resources:
            {{- toYaml .Values.backend.footPrint.resources | nindent 12 }}
          env:
            {{- include "ultragreen-dashboard.env-vars" . | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "ultragreen-dashboard.fullname" . }}-footprint
  labels:
    {{- include "ultragreen-dashboard.labels" . | nindent 4 }}
    component: footprint
spec:
  type: {{ .Values.backend.footPrint.service.type }}
  ports:
    - port: {{ .Values.backend.footPrint.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ultragreen-dashboard.selectorLabels" . | nindent 4 }}
    component: footprint
