apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ultragreen-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "ultragreen-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ultragreen-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
