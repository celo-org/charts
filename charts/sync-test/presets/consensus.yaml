---
# Consensus-layer sync: restore from snapshot, derive blocks from L1
snapshot:
  enabled: true

op-geth:
  config:
    syncmode: full
  init:
    genesis:
      enabled: false

op-node:
  config:
    syncmode: consensus-layer
