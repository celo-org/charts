{{- include "sync-test.validate" . -}}
Sync test "{{ .Release.Name }}" deployed to namespace "{{ .Release.Namespace }}".

Check pod status:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

Tail op-geth logs:
  kubectl logs -f {{ .Release.Name }}-op-geth-0 -c op-geth -n {{ .Release.Namespace }}

Tail op-node logs:
  kubectl logs -f {{ .Release.Name }}-op-node-0 -c op-node -n {{ .Release.Namespace }}

Cleanup:
  helm uninstall {{ .Release.Name }} -n {{ .Release.Namespace }}
  kubectl delete pvc data-{{ .Release.Name }}-op-geth-0 data-{{ .Release.Name }}-op-node-0 -n {{ .Release.Namespace }}

NOTE: PVCs are protected with helm.sh/resource-policy=keep and are NOT
deleted by helm uninstall. You must delete them manually (see above).
